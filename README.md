# Code source pour les OAD

Le script de calcul est inclu dans chaque fichier `HTML` entre les balises `</div>`et `</body>`. 

> Pour l'instant, seulement le code pour `code OAD Nbr piquets.html` est réalisé. 